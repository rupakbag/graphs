package com.library.graph;

public class DetectCycle {
    /*
    * This method returns the list of nodes that form a cycle
    * Returns Null if no cycle exists
     */
    public static String getCycle(Graph g) {
        //get starting node
        //start DFS. get adj nodes and keep a map of nodes and path
        
        Node n = g.nodeIterator().next();

        return "";
    }
}
